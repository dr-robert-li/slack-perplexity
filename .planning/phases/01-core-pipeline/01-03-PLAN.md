---
phase: 01-core-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .env
  - .gitignore
autonomous: false
requirements:
  - RELY-03
  - SURF-02

must_haves:
  truths:
    - "Bot connects to Slack via Socket Mode and stays online"
    - "DM to bot produces a threaded, cited answer in real Slack"
    - "No Slack timeout errors — ack fires within 3 seconds"
  artifacts:
    - path: ".env"
      provides: "Real credentials for Slack and Perplexity"
      contains: "SLACK_BOT_TOKEN"
  key_links:
    - from: "app.py"
      to: "Slack Socket Mode"
      via: "SocketModeHandler.start()"
      pattern: "SocketModeHandler"

user_setup:
  - service: slack
    why: "Bot needs Slack app credentials for Socket Mode"
    env_vars:
      - name: SLACK_BOT_TOKEN
        source: "Slack App > OAuth & Permissions > Bot User OAuth Token (xoxb-...)"
      - name: SLACK_APP_TOKEN
        source: "Slack App > Basic Information > App-Level Tokens > create token with connections:write scope (xapp-...)"
    dashboard_config:
      - task: "Enable Socket Mode"
        location: "Slack App > Socket Mode > toggle ON"
      - task: "Subscribe to message.im bot event"
        location: "Slack App > Event Subscriptions > Subscribe to Bot Events > add message.im"
      - task: "Add required scopes"
        location: "Slack App > OAuth & Permissions > Bot Token Scopes > add chat:write, im:history, im:read, im:write"
  - service: perplexity
    why: "Bot needs Perplexity API key for pro-search"
    env_vars:
      - name: PERPLEXITY_API_KEY
        source: "Perplexity Dashboard > API Keys > create key"
---

<objective>
Create .env with real credentials and perform a live smoke test — send a real DM to the bot in Slack and verify the complete pipeline works end-to-end.

Purpose: Validates RELY-03 (ack within 3s) and SURF-02 (Socket Mode) which can only be verified with a real Slack connection. Also confirms the entire Phase 1 pipeline works in production.
Output: Confirmed working bot in Slack.
</objective>

<execution_context>
@/Users/robertli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robertli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-pipeline/01-CONTEXT.md
@.planning/phases/01-core-pipeline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env file and add .gitignore</name>
  <files>.env, .gitignore</files>
  <action>
    1. Create `.gitignore` with: `.env`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `*.egg-info/`
    2. Create `.env` from `.env.example` — populate with placeholder values that clearly indicate they need real values: `SLACK_BOT_TOKEN=xoxb-your-token-here`, `SLACK_APP_TOKEN=xapp-your-token-here`, `PERPLEXITY_API_KEY=pplx-your-key-here`
    3. Print message: "Please populate .env with real credentials before the smoke test"
  </action>
  <verify>test -f .env && test -f .gitignore && echo "Files exist"</verify>
  <done>.env exists with placeholder structure. .gitignore prevents .env from being committed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Live smoke test — real DM to bot in Slack</name>
  <files>.env</files>
  <action>
    User populates .env with real credentials and tests the bot live:
    1. Populate `.env` with real credentials:
       - SLACK_BOT_TOKEN (xoxb-...) from Slack App > OAuth and Permissions
       - SLACK_APP_TOKEN (xapp-...) from Slack App > Basic Information > App-Level Tokens (needs connections:write scope)
       - PERPLEXITY_API_KEY from Perplexity Dashboard
    2. Ensure Slack app has: Socket Mode ON, message.im event subscription, scopes: chat:write, im:history, im:read, im:write
    3. Run: `python app.py`
    4. Open Slack and send a DM to the bot (e.g., "What is quantum computing?")
    5. Verify:
       - "Searching..." appears as a threaded reply (not top-level)
       - "Searching..." updates in-place to a full answer with numbered citations
       - Citations use clickable links (not raw URLs)
       - No Slack timeout/error indicators
    6. Send a second DM — verify no greeting prefix this time
    7. Type "approved" when working, or describe issues
  </action>
  <verify>User confirms "approved"</verify>
  <done>Real DM to bot produces a cited, threaded answer in Slack. Bot stays connected via Socket Mode. No timeout errors.</done>
</task>

</tasks>

<verification>
- Bot connects to Slack without errors
- DM produces threaded, cited answer
- No "dispatch_failed" in Slack (confirms ack < 3s)
- "Searching..." updates in-place (no delete-and-repost)
</verification>

<success_criteria>
- Real DM to bot produces a cited, threaded answer in Slack
- Bot stays connected via Socket Mode
- No timeout errors from Slack
- User confirms with "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-03-SUMMARY.md`
</output>
