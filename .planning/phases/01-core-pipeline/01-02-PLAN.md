---
phase: 01-core-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - handlers/__init__.py
  - handlers/dm_handler.py
  - app.py
  - tests/test_dm_handler.py
autonomous: true
requirements:
  - INTR-01
  - INTR-04
  - RESP-01
  - RESP-02
  - RELY-01
  - RELY-02
  - RELY-03

must_haves:
  truths:
    - "User sends a DM and receives a threaded reply with cited answer"
    - "A 'Searching...' indicator appears before the answer, then updates in-place"
    - "Bot ignores its own messages — no infinite loops"
    - "When Perplexity fails, bot replies with the friendly error message mentioning @Robert Li"
    - "Handler acks within 3 seconds via lazy listener pattern"
  artifacts:
    - path: "handlers/dm_handler.py"
      provides: "DM message handler with lazy listener, loading indicator, Perplexity call, citation formatting, error handling, 60s timer, first-time greeting"
      exports: ["register_dm_handler"]
    - path: "app.py"
      provides: "Updated app with DM handler registered"
      contains: "register_dm_handler"
  key_links:
    - from: "handlers/dm_handler.py"
      to: "services/perplexity.py"
      via: "query_perplexity(user_text)"
      pattern: "query_perplexity"
    - from: "handlers/dm_handler.py"
      to: "utils/formatting.py"
      via: "format_answer and split_message"
      pattern: "format_answer|split_message"
    - from: "handlers/dm_handler.py"
      to: "Slack client"
      via: "client.chat_postMessage(thread_ts=...) and client.chat_update"
      pattern: "thread_ts"
    - from: "app.py"
      to: "handlers/dm_handler.py"
      via: "register_dm_handler(app)"
      pattern: "register_dm_handler"
---

<objective>
Build the DM message handler that wires the Perplexity service layer and formatting utils into a complete Slack interaction flow: receive DM, ack immediately via lazy listener, post "Searching..." in thread, call Perplexity, update loading message with cited answer, handle errors gracefully.

Purpose: This is the core user-facing feature — the complete pipeline from question to cited answer.
Output: Working DM handler with all reliability requirements, registered in app.py.
</objective>

<execution_context>
@/Users/robertli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robertli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-pipeline/01-CONTEXT.md
@.planning/phases/01-core-pipeline/01-RESEARCH.md
@.planning/phases/01-core-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — contracts the executor needs -->

From services/perplexity.py:
```python
def query_perplexity(question: str) -> dict:
    """Returns {"answer": str, "citations": [{"title": str, "url": str}]}"""
```

From utils/formatting.py:
```python
def format_answer(answer: str, citations: list[dict]) -> str:
    """Formats answer with Slack mrkdwn footnotes. Max 5 citations."""

def split_message(text: str, limit: int = 3800) -> list[str]:
    """Splits text into chunks for Slack character limits."""
```

From app.py:
```python
from slack_bolt import App
app = App(token=os.environ.get("SLACK_BOT_TOKEN"))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DM handler with full pipeline and tests</name>
  <files>handlers/__init__.py, handlers/dm_handler.py, tests/test_dm_handler.py</files>
  <behavior>
    - test_ignores_bot_messages: When event has bot_id set, handler does not call chat_postMessage (RELY-02)
    - test_ignores_non_dm: When event has channel_type != "im", handler does not call chat_postMessage
    - test_ignores_message_subtypes: When event has subtype (e.g. "message_changed"), handler does not process
    - test_loading_message_posted: Handler calls client.chat_postMessage with text="Searching..." and thread_ts=event["ts"] (RESP-01, INTR-04)
    - test_loading_message_updated: After Perplexity returns, handler calls client.chat_update with the loading message ts and formatted answer text (RESP-02)
    - test_dm_triggers_response: Given valid DM event, handler calls query_perplexity with event text, then format_answer with result, then chat_update (INTR-01)
    - test_reply_is_threaded: All client.chat_postMessage calls include thread_ts matching the original event ts (INTR-04)
    - test_error_message: When query_perplexity raises any exception, handler calls chat_update with the friendly error message containing "@Robert Li" (RELY-01)
    - test_first_time_greeting: First message from a user_id prepends greeting text; second message from same user_id does not
    - test_long_response_split: When formatted answer exceeds 3800 chars, first chunk updates loading message, additional chunks post as new thread messages
  </behavior>
  <action>
    1. Create `handlers/__init__.py` (empty)
    2. Create `handlers/dm_handler.py` with:
       - Module-level `greeted_users = set()` for first-time detection
       - Module-level constants:
         - `ERROR_MSG = "Uh oh, it seems my brain is offline \u2014 talk to @Robert Li about trying to kick start it"`
         - `GREETING = "Hey there! I'm Kahm-pew-terr, your AI research assistant. Ask me anything and I'll search the web for a cited answer.\n\n"`
       - `handle_dm(client, event)` function (the lazy handler):
         - Extract channel, thread_ts (= event["ts"]), user_id, user_text from event
         - Post "Searching..." as threaded reply, capture loading_ts
         - Start `threading.Timer(60, update_slow_message, args=[client, channel, loading_ts])`
         - Try: call `query_perplexity(user_text)`, cancel timer immediately after
         - Determine greeting prefix (check/add user_id in greeted_users set)
         - Call `format_answer(result["answer"], result["citations"])`, prepend greeting if first time
         - Call `split_message(full_text)` to get chunks
         - `chat_update` loading message with first chunk
         - For remaining chunks: `chat_postMessage` in same thread
         - Except any Exception: cancel timer, `chat_update` loading message with ERROR_MSG
       - `update_slow_message(client, channel, loading_ts)` helper: calls `chat_update` with "Taking longer than expected, still working on it..."
       - `register_dm_handler(app)` function:
         - Defines inner `ack_dm_message(ack, event)` that checks `event.get("bot_id")`, `event.get("subtype")`, and `event.get("channel_type") != "im"` — returns without ack if any guard triggers
         - Registers via `app.event("message", lazy=[handle_dm])(ack_dm_message)`
    3. Create `tests/test_dm_handler.py` with all tests per behavior block. Use `unittest.mock.patch` to mock `query_perplexity` and `format_answer`. Use the `mock_slack_client` fixture from conftest.
    4. Run `pytest tests/test_dm_handler.py -x -v`
  </action>
  <verify>
    <automated>pytest tests/test_dm_handler.py -x -v</automated>
  </verify>
  <done>All 10 DM handler tests pass. Bot ignores own messages, posts threaded loading indicator, updates with cited answer, handles errors with friendly message mentioning @Robert Li, greets first-time users, splits long responses.</done>
</task>

<task type="auto">
  <name>Task 2: Register DM handler in app.py and run full suite</name>
  <files>app.py</files>
  <action>
    1. Update `app.py` to import and call `register_dm_handler(app)` from `handlers.dm_handler` after app initialization
    2. The import and registration should happen AFTER `app = App(...)` but BEFORE the `if __name__` block
    3. Run full test suite to confirm nothing is broken
  </action>
  <verify>
    <automated>pytest tests/ -x -v</automated>
  </verify>
  <done>app.py imports and registers DM handler. Full test suite green. The complete pipeline is wired: app.py -> dm_handler -> perplexity service -> formatting utils.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` — full suite green (all Plan 01 + Plan 02 tests)
- `handlers/dm_handler.py` contains `thread_ts=event["ts"]` for threaded replies
- `handlers/dm_handler.py` contains the exact error message with "@Robert Li"
- `handlers/dm_handler.py` checks `event.get("bot_id")` to prevent loops
- `handlers/dm_handler.py` uses lazy listener pattern
- `app.py` calls `register_dm_handler(app)`
</verification>

<success_criteria>
- DM event triggers full pipeline: ack -> loading -> Perplexity -> formatted answer (tested)
- All responses are threaded via thread_ts (tested)
- Bot ignores its own messages (tested)
- Errors produce friendly message with @Robert Li (tested)
- Loading message updates in-place (tested)
- First-time greeting works (tested)
- Long responses split correctly (tested)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-02-SUMMARY.md`
</output>
