---
phase: 01-core-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - pytest.ini
  - app.py
  - services/__init__.py
  - services/perplexity.py
  - utils/__init__.py
  - utils/formatting.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_perplexity_service.py
  - tests/test_formatting.py
  - tests/test_app.py
autonomous: true
requirements:
  - RESP-03
  - RESP-04
  - SURF-02

must_haves:
  truths:
    - "Perplexity service calls pro-search preset and returns answer text + citations"
    - "Citation formatter produces Slack mrkdwn footnotes with max 5 linked sources"
    - "App initializes with Slack Bolt in Socket Mode"
  artifacts:
    - path: "services/perplexity.py"
      provides: "Perplexity client wrapper with citation extraction"
      exports: ["query_perplexity"]
    - path: "utils/formatting.py"
      provides: "Citation formatter and message splitter"
      exports: ["format_answer", "split_message"]
    - path: "app.py"
      provides: "Bolt app init and Socket Mode entry point"
      contains: "SocketModeHandler"
    - path: "requirements.txt"
      provides: "All project dependencies"
      contains: "slack_bolt"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for mock Slack client and Perplexity response"
  key_links:
    - from: "services/perplexity.py"
      to: "perplexity SDK"
      via: "Perplexity().responses.create(preset='pro-search')"
      pattern: "preset.*pro-search"
    - from: "utils/formatting.py"
      to: "Slack mrkdwn"
      via: "<url|title> link syntax"
      pattern: "<.*\\|.*>"
---

<objective>
Scaffold the project, install dependencies, create the Perplexity service layer with citation extraction, the message formatting utilities, and the app entry point with Socket Mode. Establish test infrastructure with pytest fixtures.

Purpose: Foundation that Plan 02 (DM handler) builds on — service layer, formatting, and app shell must exist first.
Output: Working service layer with tests, formatting utils with tests, app shell, test fixtures.
</objective>

<execution_context>
@/Users/robertli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robertli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-pipeline/01-CONTEXT.md
@.planning/phases/01-core-pipeline/01-RESEARCH.md
@.planning/phases/01-core-pipeline/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding, dependencies, and Perplexity service layer</name>
  <files>requirements.txt, .env.example, pytest.ini, services/__init__.py, services/perplexity.py, tests/__init__.py, tests/conftest.py, tests/test_perplexity_service.py</files>
  <behavior>
    - test_uses_pro_search: `query_perplexity("test question")` calls `pplx_client.responses.create(preset="pro-search", input="test question")`
    - test_returns_answer_and_citations: Given a mock Perplexity response with output_text="Answer" and search_results with 3 items, `query_perplexity` returns dict with `answer` string and `citations` list of {title, url} dicts
    - test_max_five_citations: Given a response with 8 search results, only first 5 are returned
    - test_no_search_results: Given a response with no search_results item, citations list is empty
    - test_api_error_raises: When Perplexity raises APIConnectionError, RateLimitError, or APIStatusError, the function re-raises (caller handles)
  </behavior>
  <action>
    1. Create `requirements.txt` with: `slack_bolt`, `perplexityai`, `python-dotenv`, `pytest`, `pytest-mock`
    2. Create `.env.example` with placeholder keys: `SLACK_BOT_TOKEN`, `SLACK_APP_TOKEN`, `PERPLEXITY_API_KEY`
    3. Create `pytest.ini` with `[pytest]` section, `testpaths = tests`, `python_files = test_*.py`
    4. Create `services/__init__.py` (empty)
    5. Create `services/perplexity.py` with:
       - Module-level `Perplexity()` client initialization (reads PERPLEXITY_API_KEY from env automatically)
       - `query_perplexity(question: str) -> dict` function that:
         - Calls `pplx_client.responses.create(preset="pro-search", input=question)`
         - Extracts `response.output_text` as `answer`
         - Iterates `response.output` for `item.type == "search_results"`, extracts first 5 results as `[{title, url}]`
         - Returns `{"answer": str, "citations": list[dict]}`
         - Does NOT catch exceptions — lets them propagate to caller
    6. Create `tests/__init__.py` (empty)
    7. Create `tests/conftest.py` with shared fixtures:
       - `mock_perplexity_response` fixture: creates a mock response object with `.output_text` property and `.output` list containing a mock search_results item (type="search_results", results=[{title, url} x 3])
       - `mock_slack_client` fixture: MagicMock with `chat_postMessage` returning `{"ts": "1234.5678"}` and `chat_update` returning `{"ok": True}`
    8. Create `tests/test_perplexity_service.py` with tests per behavior block above
    9. Run `pip install -r requirements.txt` then `pytest tests/test_perplexity_service.py -x -v`
  </action>
  <verify>
    <automated>pytest tests/test_perplexity_service.py -x -v</automated>
  </verify>
  <done>All 5 Perplexity service tests pass. requirements.txt exists with all deps. .env.example documents required env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Citation formatter, message splitter, and app entry point</name>
  <files>utils/__init__.py, utils/formatting.py, tests/test_formatting.py, app.py, tests/test_app.py</files>
  <behavior>
    - test_format_with_citations: Given answer="Hello" and citations=[{title:"Page", url:"https://example.com"}], output is "Hello\n---\n[1] &lt;https://example.com|Page&gt;"
    - test_format_no_citations: Given answer="Hello" and citations=[], output is "Hello\n\nNo web sources found for this query"
    - test_format_multiple_citations: Given 3 citations, output has [1], [2], [3] numbered entries separated by newlines
    - test_split_short_message: Message under 3800 chars returns single-element list
    - test_split_long_message: Message of 7700 chars returns list of 3 chunks, each <= 3800 chars
    - test_app_initializes: App object can be imported from app module without error (mocked env vars)
  </behavior>
  <action>
    1. Create `utils/__init__.py` (empty)
    2. Create `utils/formatting.py` with:
       - `format_answer(answer: str, citations: list[dict]) -> str`: Takes answer text and citations list from `query_perplexity`. If citations exist, appends `\n---\n` followed by numbered entries formatted as `[N] <url|title>` (Slack mrkdwn link syntax, NOT markdown). If no citations, appends `\n\nNo web sources found for this query`. Max 5 citations enforced here as safety net.
       - `split_message(text: str, limit: int = 3800) -> list[str]`: Splits text into chunks of at most `limit` characters. Returns list with at least one element.
    3. Create `tests/test_formatting.py` with tests per behavior block
    4. Create `app.py` with:
       - `from dotenv import load_dotenv; load_dotenv()`
       - `from slack_bolt import App`
       - `from slack_bolt.adapter.socket_mode import SocketModeHandler`
       - `app = App(token=os.environ.get("SLACK_BOT_TOKEN"))` — use `.get()` so import doesn't crash in tests
       - `# Handler registration will be added in Plan 02`
       - `if __name__ == "__main__":` block that calls `SocketModeHandler(app, os.environ["SLACK_APP_TOKEN"]).start()`
    5. Create `tests/test_app.py` with:
       - `test_app_initializes`: patches `os.environ` with fake tokens, imports `app` from `app` module, asserts it's an `App` instance
    6. Run `pytest tests/ -x -v` (full suite)
  </action>
  <verify>
    <automated>pytest tests/ -x -v</automated>
  </verify>
  <done>All formatting tests and app init test pass. Full test suite green. `format_answer` uses Slack mrkdwn `<url|title>` syntax (not markdown). `split_message` handles long responses.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` — all tests green
- `services/perplexity.py` contains `preset="pro-search"`
- `utils/formatting.py` uses `<url|title>` Slack mrkdwn syntax (NOT `[title](url)`)
- `app.py` contains `SocketModeHandler`
- `.env.example` lists all 3 required env vars
</verification>

<success_criteria>
- Perplexity service layer queries pro-search and extracts citations (tested)
- Formatter produces Slack mrkdwn footnotes with max 5 sources (tested)
- Message splitter handles responses over 3800 chars (tested)
- App initializes with Bolt + Socket Mode (tested)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-01-SUMMARY.md`
</output>
